(ns printer3d.disco-tommi
  (:require [printer3d.utils :as utils]
            [scad-clj.scad :as scad]
            [scad-clj.model :as model]))



(def ring-inner-diam 18.5)


(def ring (model/cylinder))




(utils/save-file "ring-1.scad" [])

